name: almanac
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.13.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # State management. Chosen for the whole app going forward — see
  # ARCHITECTURE.md for rationale.
  flutter_riverpod: ^3.4.3

  # Declarative routing with support for a persistent bottom-navigation
  # shell (StatefulShellRoute), which the primary navigation needs.
  go_router: ^18.0.1

  # Local storage for the small set of user preferences (currently just the
  # chosen hemisphere). Maintained by the Flutter team. Uses
  # SharedPreferencesWithCache so reads are synchronous after one async
  # open, which lets startup decide about onboarding without a loading flash.
  shared_preferences: ^2.5.5

  # Location permission and position. Separates checkPermission() (never
  # prompts) from requestPermission() (prompts), and reports permanent
  # denial — which is what the app's location state model is built on.
  # Only "while in use" is ever requested; never background location.
  geolocator: ^14.0.3
  timezone: ^0.11.1
  flutter_timezone: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  # Test-only, and already in the tree as a transitive dependency of
  # shared_preferences: it carries the plugin's own in-memory
  # implementation, which lets the Cycle store's real serialisation and
  # its delete-everything be tested rather than mocked. Nothing in
  # `lib/` imports it.
  shared_preferences_platform_interface: ^2.4.2

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # THE BUNDLED-FONT SLOT.
  #
  # The Almanac names no typeface anywhere in `lib/` except
  # `lib/app/theme/almanac_fonts.dart`, which currently resolves to the
  # platform's own serif and UI faces. To give the app the hand-lettered
  # display face the visual references are set in:
  #
  #   1. put the licensed files in `assets/fonts/`;
  #   2. uncomment and complete the block below;
  #   3. set `AlmanacFonts.bundledDisplay` to the same family name.
  #
  # Nothing else changes. The face MUST be bundled, never fetched: the
  # `google_fonts` package was removed in Step 17 precisely because it
  # downloaded its fonts at runtime, which this app must never do.
  #
  # fonts:
  #   - family: AlmanacDisplay
  #     fonts:
  #       - asset: assets/fonts/AlmanacDisplay-Regular.ttf
  #       - asset: assets/fonts/AlmanacDisplay-Medium.ttf
  #         weight: 500
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
